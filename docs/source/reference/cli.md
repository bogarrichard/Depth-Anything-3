# CLI reference

Every option below is rendered directly from the `da3` CLI's own Typer/Click
definitions, so it can't drift out of sync with what `--help` prints. See
{doc}`../usage/cli` for a task-oriented walkthrough with example commands.

```{eval-rst}
.. typer:: depth_anything_3.cli:app
   :prog: da3
   :show-nested:
   :width: 100
```
