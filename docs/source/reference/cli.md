# Command Line Interface

One section per command below, each rendered directly from the `da3` CLI's
own Typer/Click definitions, so it can't drift out of sync with what
`--help` prints. See {doc}`export_formats` for what each `--export-format`
name actually does, and {doc}`../cli_examples` for task-oriented example
commands.

```{eval-rst}
.. typer-reference:: depth_anything_3.cli:app
   :prog: da3
```
