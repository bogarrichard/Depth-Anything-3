# Sphinx build tooling only. The package itself (core dependencies, incl.
# torch) is installed separately -- see .readthedocs.yaml, which points pip
# at the CPU wheel index so the build doesn't pull a multi-gigabyte CUDA
# build of torch it will never use.
sphinx>=7
myst-parser>=2
sphinx-rtd-theme>=2
sphinx-design>=0.5
sphinxcontrib-typer>=0.5
sphinx-autodoc-typehints>=2
